<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="check" type="com.flashbox.balitrip2019.checklist.CheckList"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:context=".checklist.ChecklistFragment">

        <CheckBox
                app:layout_constraintEnd_toEndOf="parent"
                android:checked="@{check.value}"
                android:layout_marginStart="16dp"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="6dp"
                android:text="@{check.name}"
                android:layout_marginEnd="16dp"
                android:textSize="16sp"
                tools:text="Shorts"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_width="0dp"
                android:layout_height="wrap_content" android:fontFamily="@font/raleway"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>